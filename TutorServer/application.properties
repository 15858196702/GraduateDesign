# 核心配置文件
# jar执行时使用参数 --spring.profiles.active=[dev|pro] 指定配置

# idea运行时指定配置
spring.profiles.active=pro

spring.datasource.url=jdbc:mysql://127.0.0.1:3306/tutor_manager?characterEncoding=UTF-8&serverTimezone=GMT%2B8
spring.datasource.username=root
spring.datasource.password=deansql
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.hbm2ddl.auto=update

# 开启数据库表列名与实体类之间的驼峰映射
mybatis.configuration.map-underscore-to-camel-case=true